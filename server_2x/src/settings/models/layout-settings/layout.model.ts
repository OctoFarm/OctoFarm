import {LayoutEntryModel} from "./layout-entry.model";

// Note: can be expanded to interface, but will break some code (which is fine)
export type LayoutModel = LayoutEntryModel[];

// export interface LayoutModel {
//     layout: LayoutEntryModel[];
// }